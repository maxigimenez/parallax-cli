save-exact=true
# Link internal @sentinel0/* packages from the workspace by version (they are
# unpublished and only bundled into sentinel0), so package.json can pin
# concrete versions instead of the workspace:* protocol.
link-workspace-packages=true